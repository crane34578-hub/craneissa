# Atlas Insight

A lightweight, single-page concept app that blends:
- **Google Maps-style interaction** (grid navigation + layer filtering)
- **Palantir-style operations intelligence** (risk scoring + timeline + recommendations)

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
